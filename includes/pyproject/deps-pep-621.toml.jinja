{%- if use_test -%}
tests = [
    "pytest",
    "pytest-cov",
    "pytest-raises",
    "pytest-randomly",
    "pytest-xdist",
]
{% endif %}
{%- if use_lint -%}
style = [
    "pydoclint",
    "ruff",
]
{% endif %}
{%- if use_types -%}
types = [
    "mypy",
]
{% endif -%}
audit = [
    "pip-audit",
]
